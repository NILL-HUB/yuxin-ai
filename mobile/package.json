{
  "name": "yuxin-ai-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "钰心AI 手机 App（Capacitor 封装 Web UI）",
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios",
    "build": "cd ../ui && npm run build && cd ../mobile && cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
