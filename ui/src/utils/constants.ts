/**
 * 默认图片配置
 * 使用 picsum.photos 提供的随机图片服务
 * 每次访问都会返回不同的图片
 */

// 通用默认图标
export const DEFAULT_ICON_URL = 'https://picsum.photos/400'

// 应用默认图标
export const DEFAULT_APP_ICON_URL = 'https://picsum.photos/400?random=app'

// 工作流默认图标
export const DEFAULT_WORKFLOW_ICON_URL = 'https://picsum.photos/400?random=workflow'

// 数据集默认图标
export const DEFAULT_DATASET_ICON_URL = 'https://picsum.photos/400?random=dataset'

// 工具默认图标
export const DEFAULT_TOOL_ICON_URL = 'https://picsum.photos/400?random=tool'

// 用户头像默认图标
export const DEFAULT_AVATAR_URL = 'https://picsum.photos/400?random=avatar'
