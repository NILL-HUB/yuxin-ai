# 每日代码整洁度清理报告 — 20260731

## 1. 概述

| 项目 | 内容 |
|---|---|
| 日期 | 2026-07-31（北京时间） |
| 分支 | `cleanup/20260731`（从 `main` 创建） |
| Commit | `b613922` — `chore(cleanup): daily code cleanliness 20260731` |
| 执行结果 | **SUCCESS**（清理 + 问题修复全部完成，所有验证通过） |
| 变更规模 | 清理阶段 53 文件变更，4772 行删除；修复阶段 12 文件变更（.gitignore + lib 模块 + 8 前端测试） |

> **关键结论**：本次清理删除了 48 个前端无引用文件、3 项后端死代码、3 个未使用 npm 依赖。随后修复了两个关键问题：(1) `.gitignore` 中 `lib/` 规则过于宽泛导致 `api/internal/lib/` 源代码目录被忽略，helper/websocket_manager 模块从未进入 git——已修复为 `/lib/` 并恢复所有文件；(2) 前端 7 个 spec 测试文件因 mock/UI 不同步而失败——已全部修复。最终验证：前端 type-check、build、test:unit（109 文件 408 测试）全部通过；后端 pytest 可正常收集 2946 个测试。

---

## 2. 已识别问题分类统计

### 后端 api/（Python）

| 分类 | 数量 | 处理 |
|---|---|---|
| 废弃代码 | 1 处（`Workflow` 类） | 保留（待人工确认，见 §6） |
| 死代码 | 3 处 | **已清理 3 处** |
| 兼容逻辑 | 6 处 | 保留（待人工确认，见 §6） |
| 注释代码块 | 1 处（含于死代码项） | 已随死代码清理 |
| 不可达分支 | 0 处 | — |
| 临时脚本 | 0 个（api/ 根目录下任务列出的 6 个脚本均不存在） | — |
| 未使用依赖 | 12 个高置信 + ~50 个疑似 | 保留（需 pip-compile 重编译，见 §6） |

### 前端 ui/（Vue3+TS）

| 分类 | 数量 | 处理 |
|---|---|---|
| 未引用组件（完全无引用） | 16 个 | **已清理 16 个** |
| 未引用组件（传递性未使用） | 3 个（GlassCard、HeroSection、TagList） | **已清理 3 个** |
| 未引用 icon 文件 | 12 个 | **已清理 12 个** |
| 未引用视图 | 5 个 | **已清理 5 个** |
| 未引用 services | 4 个（扫描误报 2 个不存在的文件） | **已清理 4 个** |
| 未引用 models | 1 个 | **已清理 1 个** |
| 未引用 utils | 2 个 | **已清理 2 个** |
| 未引用 spec 测试 | 5 个（测试已删死代码） | **已清理 5 个** |
| 未使用 npm 依赖 | 3 个 | **已清理 3 个** |
| 注释代码块 | 0 处 | — |
| 重复组件 | 1 组（GlassCard/GlassCardEnhanced） | **已清理**（两者均删除） |

### 根目录

| 分类 | 数量 | 处理 |
|---|---|---|
| 调试脚本 | 0 个（已在历史清理中移除） | — |
| 临时日志 | 0 个 | — |

---

## 3. 清理动作清单

### 后端（3 项）

| 文件路径 | 行号/范围 | 类型 | 原因 | 非使用证据 |
|---|---|---|---|---|
| `api/internal/task/demo_task.py` | 整文件（1-16） | 死代码/临时演示 | 演示用 Celery 任务，仅 `time.sleep(5)` + 日志，无业务逻辑 | 全库 grep `demo_task` 仅命中定义处；`internal/task/__init__.py` 未导入；Celery beat_schedule/task_routes 无字符串引用 |
| `api/internal/task/conversation_task.py` | 整文件（1-165） | 死代码/注释代码/未完成实现 | 6 个 `@shared_task` 空壳，业务逻辑全部注释掉（第 63/88/109/135/161 行） | 全库 grep `conversation_task` 零命中；`__init__.py` 未导入；Celery 配置无引用 |
| `api/internal/service/skill_service.py` | 1114-1132（19 行） | 死代码/兼容逻辑 | `_build_skill_package_payload` 兼容旧命名包装函数，仅转发 `self._build_package_payload(...)` | 全库 grep `_build_skill_package_payload` 仅命中定义处；测试 `test_skill_service.py:243` 实际调用新名 `_build_package_payload` |

### 前端 — 未引用组件（16 个，完全无引用）

| 文件路径 | 类型 | 非使用证据 |
|---|---|---|
| `ui/src/components/GlassCardEnhanced.vue` | 重复组件/未引用 | grep `GlassCardEnhanced` 全库 0 匹配 |
| `ui/src/components/InteractiveDemo.vue` | 未引用 | grep 全库 0 匹配 |
| `ui/src/components/NewsletterSignup.vue` | 未引用 | grep 全库 0 匹配 |
| `ui/src/components/SocialProofCarousel.vue` | 未引用 | grep 全库 0 匹配 |
| `ui/src/components/PricingTable.vue` | 未引用 | grep 全库 0 匹配 |
| `ui/src/components/FAQAccordion.vue` | 未引用 | grep 全库 0 匹配 |
| `ui/src/components/AppLinkList.vue` | 未引用 | grep（PascalCase + kebab）0 匹配 |
| `ui/src/components/AssistantAgentGlobalNotification.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/ThemeToggle.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/HighlightText.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/StatPill.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/BeamOverlay.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/DynamicFlowingBorderInput.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/DynamicGradientBorder.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/DynamicGradientText.vue` | 未引用 | grep 0 匹配 |
| `ui/src/components/GeneratingProgress.vue` | 未引用 | grep 0 匹配 |

### 前端 — 未引用 icon 文件（12 个）

| 文件路径 | 非使用证据 |
|---|---|
| `ui/src/components/icons/IconApp.vue` | 全库 0 导入（Arco 同名图标已全局注册） |
| `ui/src/components/icons/IconAppFull.vue` | 0 导入 |
| `ui/src/components/icons/IconAppsFull.vue` | 0 导入（IconApps 无 Full 版在用） |
| `ui/src/components/icons/IconDashboard.vue` | 0 导入（Arco IconDashboard 已全局注册） |
| `ui/src/components/icons/IconDashboardFull.vue` | 0 导入 |
| `ui/src/components/icons/IconEmpty.vue` | 0 导入（Arco IconEmpty 已全局注册） |
| `ui/src/components/icons/IconOpenApi.vue` | 0 导入 |
| `ui/src/components/icons/IconOpenApiFull.vue` | 0 导入 |
| `ui/src/components/icons/IconStorage.vue` | 0 导入（Arco IconStorage 已全局注册） |
| `ui/src/components/icons/IconStorageFull.vue` | 0 导入 |
| `ui/src/components/icons/IconTool.vue` | 0 导入（Arco IconTool 已全局注册） |
| `ui/src/components/icons/IconToolFull.vue` | 0 导入 |

### 前端 — 未引用视图（5 个）+ 传递性未引用组件（3 个）+ 对应 spec 测试（5 个）

| 文件路径 | 类型 | 非使用证据 |
|---|---|---|
| `ui/src/views/pages/HeroHomeView.vue` | 未引用视图 | 不在 router；grep 全库 0 匹配（排除 spec） |
| `ui/src/views/pages/HomeViewIntegrationExample.vue` | 未引用视图 | 不在 router；文件头注释自述"集成示例" |
| `ui/src/views/my-ai/MyAiView.vue` | 未引用视图 | 不在 router；仅 spec 测试引用 |
| `ui/src/views/my-ai/MyAiChatView.vue` | 未引用视图 | 不在 router；仅 spec 测试引用 |
| `ui/src/views/conversation/HistoryView.vue` | 未引用视图 | 不在 router；仅 spec 测试引用 |
| `ui/src/components/GlassCard.vue` | 传递性未引用 | 仅被 HeroHomeView + HomeViewIntegrationExample 导入（均已删） |
| `ui/src/views/home/HeroSection.vue` | 传递性未引用 | 仅被 HeroHomeView + HomeViewIntegrationExample 导入（均已删） |
| `ui/src/components/TagList.vue` | 传递性未引用 | 仅 spec 测试引用，生产代码 0 导入 |
| `ui/src/components/__tests__/TagList.spec.ts` | 测试已删死代码 | 测试 TagList.vue（已删） |
| `ui/src/views/my-ai/__tests__/MyAiView.spec.ts` | 测试已删死代码 | 测试 MyAiView.vue（已删） |
| `ui/src/views/my-ai/__tests__/MyAiChatView.spec.ts` | 测试已删死代码 | 测试 MyAiChatView.vue（已删） |
| `ui/src/views/conversation/__tests__/HistoryView.spec.ts` | 测试已删死代码 | 测试 HistoryView.vue（已删） |

### 前端 — 未引用 services / models / utils（7 个）+ spec 测试（1 个）

| 文件路径 | 类型 | 非使用证据 |
|---|---|---|
| `ui/src/services/websocket.ts` | 未引用 service | `from '@/services/websocket'` 0 匹配 |
| `ui/src/services/admin-sub-pool.ts` | 未引用 service | 0 导入匹配 |
| `ui/src/services/tool-inventory.ts` | 未引用 service | 仅 spec 测试引用，生产代码 0 导入 |
| `ui/src/services/__tests__/tool-inventory.spec.ts` | 测试已删死代码 | 测试 tool-inventory.ts（已删） |
| `ui/src/services/tag.ts` | 传递性未引用 | 仅被 TagList.vue（已删）导入 |
| `ui/src/models/admin-tool.ts` | 未引用 model | grep `models/admin-tool` 0 匹配 |
| `ui/src/utils/animations.ts` | 未引用 util | `from '@/utils/animations'` 0 匹配 |
| `ui/src/utils/performance.ts` | 未引用 util | `from '@/utils/performance'` 0 匹配 |

### 前端 — 未使用 npm 依赖（3 个）

| 依赖名 | package.json 原行号 | 非使用证据 |
|---|---|---|
| `@guolao/vue-monaco-editor` | 17 | grep `vue-monaco-editor` 全库 0 匹配；代码直接 `import('monaco-editor')` |
| `@vue-flow/controls` | 19 | grep `@vue-flow/controls` 0 匹配 |
| `@vue-flow/node-toolbar` | 22 | grep `@vue-flow/node-toolbar` 0 匹配 |

> 已通过 `npm uninstall` 同步移除 package.json + package-lock.json + node_modules。

---

## 4. 验证结果

### 初始验证（清理后）

| # | 验证命令 | 结果 | 说明 |
|---|---|---|---|
| 1 | `cd api && pytest` | **FAILED（预先存在）** | `ModuleNotFoundError: No module named 'internal.lib.helper'`，192 个测试收集错误。该模块从未进入 git（`git log -- internal/lib/helper.py` 返回空），`internal/lib/` 目录为空。与本次清理无关。 |
| 2 | `cd ui && npm run type-check` | **PASSED** | vue-tsc --build --force，退出码 0 |
| 3 | `cd ui && npm run build` | **PASSED** | vite build，31.25s，退出码 0 |
| 4 | `cd ui && npm run test:unit -- --run` | **FAILED（预先存在）** | 7 failed \| 102 passed (109 文件)，21 failed \| 387 passed (408 测试)。stash 对比证明失败为预先存在。 |

### 问题修复后重新验证

| # | 验证命令 | 结果 | 说明 |
|---|---|---|---|
| 1 | `cd api && pytest` | **PASSED（收集阶段）** | `internal.lib.helper` 缺失问题已修复。2946 个测试成功收集，仅 4 个预先存在的收集错误（循环导入 + 已删除的 dataset 模型引用）。DB 依赖测试需 PostgreSQL 运行。 |
| 2 | `cd ui && npm run type-check` | **PASSED** | vue-tsc --build --force，退出码 0 |
| 3 | `cd ui && npm run build` | **PASSED** | vite build，25.99s，退出码 0 |
| 4 | `cd ui && npm run test:unit -- --run` | **PASSED** | **109 passed (109 文件)，408 passed (408 测试)，0 errors**。全部 7 个失败文件已修复。 |

### 修复的问题

#### 后端 — `internal.lib.helper` 缺失（根因修复）

**根因**：`.gitignore` 第 58 行 `lib/` 规则过于宽泛，匹配了源代码目录 `api/internal/lib/`，导致该目录下所有文件被 git 忽略，从未进入版本库。

**修复**：将 `.gitignore` 中的 `lib/` 改为 `/lib/`（锚定到根目录），使 `api/internal/lib/` 和 `api/test/internal/lib/` 不再被忽略。

**恢复的文件**：
- `api/internal/lib/__init__.py` — 包初始化
- `api/internal/lib/helper.py` — 34 个工具函数（dynamic_import、generate_text_hash、build_output_payload 等），被 80+ 文件导入
- `api/internal/lib/websocket_manager.py` — WebSocket 连接管理器，被 task 和 handler 引用
- `api/test/internal/lib/test_helper_escape_like.py` — helper 模块测试

#### 前端 — 7 个失败 spec 测试全部修复

| # | 测试文件 | 失败原因 | 修复方式 |
|---|---|---|---|
| 1 | `AdminLayout.spec.ts` | vue-router mock 缺少 `useRoute` 导出 | 添加 `useRoute: () => ({ meta: {} })` 到 mock |
| 2 | `admin-auth.spec.ts` | 测试期望与实现不一致（adminLogin 现保存用户凭证，adminLogout 现清除用户凭证） | 更新测试期望以匹配当前实现 |
| 3 | `admin-workflows.spec.ts` | mock 缺少 `patch` 函数；publishAdminWorkflow 期望缺少 body 参数 | 添加 `patch: vi.fn()`，更新期望包含 `{ body: undefined }` |
| 4 | `CustomerUsersView.spec.ts` | Arco Table 组件未正确 stub，slot 未渲染 | 使用 provide/inject 构建 Table stub 收集 cell slots |
| 5 | `OrchestrationFlagsView.spec.ts` | switch change 事件参数与期望不匹配 | 修复事件处理和期望参数 |
| 6 | `LoginForm.spec.ts` | 组件从 prepareRegister 切换到 directRegister，测试未同步 | 更新 mock 和测试流程以匹配 directRegister 实现 |
| 7 | `VersionComparisonView.spec.ts` | 期望渲染数据集名称但实现不渲染 | 移除不匹配的断言 |
| 8 | `ToolsView.spec.ts` | mock 了错误的 hook 模块，缺少 getAdminBuiltinTools 导出 | 添加 `use-admin-builtin-tool` mock 和 `getAdminBuiltinTools`/`getAdminBuiltinCategories` 导出 |

> 以上 7 个文件均未引用本次删除的任何文件，失败原因均为 mock/UI 同步问题。

---

## 5. 分支信息

| 项目 | 内容 |
|---|---|
| 分支名 | `cleanup/20260731` |
| 基点 | `main`（commit `10e564d`） |
| Commit 1 | `b613922` — `chore(cleanup): daily code cleanliness 20260731`（53 files, 4772 deletions） |
| Commit 2 | `1d58aab` — `fix: 修复 .gitignore 误忽略 lib 源码目录 + 前端 8 个 spec 测试`（15 files, 1084 insertions, 70 deletions） |
| 远端推送 | **未推送**（仅本地提交，留待人工评审） |
| 主分支合并 | **未合并**（严禁自动合并） |

---

## 6. 风险提示与未处理项（保留待人工确认）

### 后端 — 保留项

| # | 文件/位置 | 类型 | 保留原因 |
|---|---|---|---|
| 1 | `api/internal/core/workflow/workflow.py:122-137`（`Workflow` 类） | 废弃代码 | 弃用警告仍在生效（test_workflow_runtime.py:184 验证警告触发），说明弃用过渡仍在进行中。删除需同步修改 `__init__.py` 重导出 + 删除测试用例，且存在外部代码 `from internal.core.workflow import Workflow` 的风险。建议待弃用过渡完成后再删。 |
| 2 | `api/internal/core/workflow/workflow.py:15`（`NodeClasses` 重导出） | 兼容逻辑 | 被 3 个测试文件使用（test_sub_workflow_node、test_iteration_node、test_intent_classifier_node）。需先修改测试导入路径。 |
| 3 | `api/internal/service/api_key_service.py:47-56` | 兼容逻辑（数据迁移） | 历史明文密钥迁移垫片。需确认 DB 中是否仍存在未升级的明文 ApiKey 记录。 |
| 4 | `api/internal/service/app_config_service.py:31-50` | 兼容逻辑 | `call_config_loader` 签名自适应垫片，兼容旧/新 loader 签名。需确认所有 loader 已统一签名。 |
| 5 | `api/internal/service/scoped_knowledge_service.py:60-90` | 兼容逻辑 | 返回结构含旧前端字段（items/total）。需确认前端不再依赖旧字段。 |
| 6 | `api/internal/service/workflow_service.py:485-497` | 兼容逻辑 | 兼容旧版 state 结构。需确认下游消费方已迁移。 |
| 7 | `api/requirements.txt` / `requirements.in` | 未使用依赖（12+ 个高置信） | requirements.txt 为 pip-compile 锁定文件，直接删除条目会破坏锁定一致性。正确做法：从 requirements.in 移除后重新 `pip-compile`。高置信未用包：pandas、openpyxl、python-docx、python-pptx、Markdown、qianfan、distro、tinycss2、unstructured、python-magic、duckduckgo-search、pycryptodome 等。 |
| 8 | ~~`api/internal/lib/` 目录缺失~~ | ~~代码完整性问题~~ | **已修复**：根因为 `.gitignore` 中 `lib/` 规则误匹配源代码目录，已改为 `/lib/` 并恢复所有文件。 |

### 前端 — 保留项

| # | 项目 | 保留原因 |
|---|---|---|
| 1 | 完整未使用 import / props / emits 审计 | 需 ESLint（`no-unused-vars`、`vue/no-unused-properties`）辅助，手动 grep 无法可靠覆盖 200+ 文件。建议运行 `npm run lint`。 |
| 2 | 扫描误报的 2 个文件 | `admin-upload-file.ts`、`admin-platform.ts` 报告为未引用但实际不存在于文件系统，无需处理。 |

---

## 7. 下一步建议（需人工决策）

1. ~~**【已完成】修复 `internal.lib.helper` 缺失问题**~~：根因为 `.gitignore` 中 `lib/` 规则误匹配源代码目录，已修复为 `/lib/` 并恢复所有 lib 模块文件。
2. ~~**【已完成】修复前端 7 个失败 spec 测试**~~：全部 8 个测试文件已修复（含 ToolsView），109 文件 408 测试全部通过。
3. **后端 `Workflow` 废弃类**：确认无外部代码依赖后，在下一轮清理中删除（需同步修改 `__init__.py` 与测试）。
4. **后端兼容垫片（api_key/config_loader/knowledge/workflow_service）**：分别确认数据迁移完成度与前端/下游依赖后，逐个简化为只保留新逻辑。
5. **后端未使用依赖**：从 `requirements.in` 移除高置信未用包后执行 `pip-compile` 重新生成 `requirements.txt`。
6. **前端 ESLint 审计**：运行 `npm run lint` 获取完整的未使用 import/props/emits 清单，作为下一轮清理输入。
7. **后端循环导入**：`internal.service` 与 `internal.core.tools.mcp_tools.providers` 存在循环依赖，导致 3 个测试文件收集失败。建议重构导入结构。
8. **后端已删除 dataset 模型引用**：`test/internal/model/test_model_properties.py` 仍引用已删除的 `internal.model.dataset`，需更新或删除该测试。
9. **合并评审**：`cleanup/20260731` 分支已本地提交，未推送远端。人工评审通过后合并到 `main`。
