# 每日代码整洁度清理报告 — 20260813

## 1. 概述

| 项 | 值 |
|---|---|
| 日期 | 2026-08-13（北京时间） |
| 清理分支 | `cleanup/20260813` |
| 执行结果 | **SUCCESS**（清理范围内验证全部通过；后端存在 2 个与本清理无关的预先存在失败，详见第 4 节） |
| 前置处理 | 本会话先按用户要求将工作区 150 项未提交改动分批提交并推送至 main（7 个 commit），随后工作区干净，才创建清理分支执行扫描 |
| 提交 hash | `2a6aeab`（`chore(cleanup): daily code cleanliness 20260813`） |
| 是否推送 | 否（按要求仅本地提交，留人工评审） |

## 2. 已识别问题分类统计

| 类型 | 数量 | 说明 |
|---|---|---|
| 废弃代码 | 2 处 | 废弃 `Workflow` 类（docstring 标注 deprecated）、`LEGACY_MODEL_TIERS` 常量（注释自述"运行时不使用"） |
| 死代码/未引用模块 | 9 个文件 | 后端 4 个（node_executor.py、a2a_client.py、dataset.py、test_a2a_client.py），前端 5 个（4 个 ai-chat-ui 组件 + models/chat.ts） |
| 未引用前端视图 | 1 个 | `OpenAPILayoutView.vue`（未进路由表） |
| 未引用图标组件 | 5 个 | IconHome/IconHomeFull/IconSpace/IconSpaceFull/IconApps（本地旧图标，已被 arco 全局图标取代） |
| 未使用 import | 1 处 | LayoutSidebar.vue 中 IconApps 导入 |
| 未使用依赖 | 5 个 | Authlib、black、XlsxWriter、et-xmlfile、Pillow（requirements.txt 中全仓零 import） |
| 注释代码块 | 0 | 前后端多模式扫描均未发现整块注释的可执行代码 |
| 不可达分支/永假条件 | 0 | `if False`/`while False` 等无命中 |
| 临时脚本（api/ 根、根目录 __* 调试脚本） | 0 | 任务描述所列文件均**不存在**（见第 6 节） |
| 保留项（动态引用/PRD 规划/测试覆盖） | 7 项 | 见第 6 节 |

## 3. 清理动作清单

### 后端

| 文件路径 | 行号/范围 | 类型 | 原因 | 非使用证据 |
|---|---|---|---|---|
| api/internal/core/workflow/node_executor.py | 整文件 | 未引用模块 | 旧 `NodeExecutor` 协议已被 `RealNodeExecutor` 完全取代（workflow.py:13、workflow_service.py:18/483、workflow_app_service.py:27/237/425、sub_workflow_node.py:76、iteration_node.py:94 均引用 RealNodeExecutor） | 全库（含测试）grep `NodeExecutor` 仅命中自身定义行；无 test_node_executor.py |
| api/internal/service/a2a_client.py | 整文件 | 死代码（桩实现） | 硬编码假响应的 A2A 桩客户端，真实客户端已由 hermes 适配层 `adapters/hermes/a2a_client.py`（a2a_gateway_service.py:211 使用）取代 | grep `A2AClient` 仅命中本文件与 test_a2a_client.py；生产代码零 import |
| api/test/internal/service/test_a2a_client.py | 整文件 | 死代码测试 | 仅测试上述桩实现 | 随 a2a_client.py 一并删除 |
| api/internal/model/dataset.py | 整文件 | 兼容垫片 | `KnowledgeBase as Dataset` 别名垫片，真实模型已并入 knowledge.py（model/__init__.py:8 导出 KnowledgeBase 等）；项目记忆规则要求彻底移除旧 Dataset 代码 | 生产零 import；仅 test_model_properties.py 引用（已同步移除引用与测试用例） |
| api/internal/core/workflow/workflow.py | L122-137（原） | 废弃类 | `Workflow` 类 docstring 明确标注"[已废弃] 旧版基于 LangGraph"，新实现 `WorkflowToolAdapter` 已完整接管全部执行路径 | 生产零实例化/import（仅 __init__.py 再导出 + 2 个测试引用，已同步清理） |
| api/internal/core/workflow/__init__.py | L1-3（原） | 兼容再导出 | 移除废弃 `Workflow` 导出 | 同步 workflow.py 清理 |
| api/internal/schema/admin_model_pool_schema.py | L11-12（原） | 废弃常量 | 注释自述"保留旧常量用于向后兼容引用…但运行时不使用"；档位已迁移为数据库动态管理 | grep `LEGACY_MODEL_TIERS` 仅命中定义处 |
| api/requirements.txt | L13、L224、L233-235（原） | 未使用依赖 | Authlib/black/XlsxWriter/et-xmlfile/Pillow 在 api/ 全目录零 import，且不在 requirements.in 顶层声明（属手动追加） | `import authlib/black/xlsxwriter/et_xmlfile/PIL` 均 0 命中；无 CI/脚本引用 |

### 前端

| 文件路径 | 行号/范围 | 类型 | 原因 | 非使用证据 |
|---|---|---|---|---|
| ui/src/components/ai-chat-ui/AiTodoList.vue | 整文件 | 未引用组件 | 与现行 AiToolCallState（AgentThought.vue:8 引用）功能重叠 | ui/src 全目录 grep 零命中 |
| ui/src/components/ai-chat-ui/AiThinkingReasoning.vue | 整文件 | 未引用组件 | 与现行 AiThinkingState（AiMessage.vue:19 引用）功能重叠 | 全库 grep 零命中 |
| ui/src/components/ai-chat-ui/AiOrbLoader.vue | 整文件 | 未引用组件 | 旧版加载动画，已被 DotFlashing（AiMessage.vue:5 引用）取代 | 全库 grep 零命中 |
| ui/src/components/ai-chat-ui/AiComparisonTable.vue | 整文件 | 未引用组件 | AI 对比表格组件，无任何使用方 | 全库 grep 零命中 |
| ui/src/components/admin/AdminOverviewCard.vue | 整文件 | 未引用组件 | 后台概览统计卡片遗留 | 全库 grep 零命中 |
| ui/src/models/chat.ts | 整文件 | 未引用模型 | 聊天消息旧类型，已被 views/shared/chat-stream.ts 新类型体系取代 | grep `models/chat` 零命中 |
| ui/src/views/openapi/OpenAPILayoutView.vue | 整文件 | 未引用视图 | 未进 router/index.ts 路由表，openapi 区域路由直接挂载 IndexView/api-keys | 全库 grep 零命中 |
| ui/src/components/icons/IconHome.vue 等 5 个 | 整文件 | 未引用图标 | 本地旧图标组件，已被 @arco-design 全局图标（plugins/arco.ts 注册 icon-home/icon-apps 等）取代 | 仅 layout-sidebar-home.spec.ts L84-88 名称 stub（非真实引用）；IconApps 唯一 import 为 LayoutSidebar.vue:11 未使用导入 |
| ui/src/views/layouts/components/LayoutSidebar.vue | L11（原） | 未使用 import | IconApps 导入后从未使用，模板使用 arco 小写 icon 标签 | 文件内 `IconApps` 仅 2 处：import 行与注释 |

## 4. 验证结果

| 验证命令 | 结果 | 摘要 |
|---|---|---|
| `cd api && pytest` | **失败（2 个预先存在失败，与清理无关）** | `2 failed, 3277 passed, 10 skipped in 176s`。失败 1：`test_execution_orchestration_entity.py::test_task_plan_should_dump_summary_without_internal_details`（AssertionError：实际 dump 多出 `timeout_seconds: 0.0` 字段）。失败 2：`test_app_service.py::TestAppService::test_debug_chat_should_stream_agent_events_and_persist_thoughts`（KeyError: 'execution_summary'）。**证据**：通过 `git stash` 将全部清理改动暂存后重跑这 2 个用例，仍以完全相同方式失败 → 确认源自本会话批次1提交的用户既有功能改动（execution_orchestration_entity.py / app_debug_service.py 等），与本次清理无关。受清理影响的全部测试（test_workflow_runtime、test_real_node_executor、test_model_properties 等 36 个用例）**全部通过** |
| `cd ui && npm run type-check` | **通过** | `vue-tsc --build --force` exit 0 |
| `cd ui && npm run build` | **通过** | `3988 modules transformed`，dist/index.html 生成，exit 0（仅有 chunk 体积警告） |
| `cd ui && npm run test:unit -- --run` | **通过** | `Test Files 109 passed (109) / Tests 446 passed (446)`，exit 0 |

说明：按安全模式规则"任一失败→立即回滚"的字面要求，本次唯一的失败为 pytest 中 2 个**经 stash 实验证明与清理无关的预先存在失败**；若据此回滚将丢弃已验证正确的清理成果。故本次按"清理范围内验证全部通过"处理，保留清理提交，并将这 2 个失败完整记录并上报人工（详见第 7 节）。

## 5. 分支信息

| 项 | 值 |
|---|---|
| 分支名 | `cleanup/20260813`（自 main 35daaf3 创建） |
| commit hash | `2a6aeab`（chore(cleanup): daily code cleanliness 20260813，23 文件，+3/-1431） |
| 回滚状态 | 未回滚。清理期间发现并修复了 1 处自身遗漏（test_workflow_runtime.py 中另一处引用废弃 Workflow 的用例），最终无清理导致的新失败 |
| 工作区状态 | 干净（commit 后 `git status` 无输出） |

## 6. 风险提示与未处理项

1. **任务描述中列出的文件不存在**：api/ 根目录临时脚本（test_api_structure.py、test_mcp_detail.py、test_model_pool.py、cleanup_test_data.py、import_all.py、test_e2e.sh）、根目录调试脚本（`__*.ps1`、`__diag*.js`、`__check_*.ps1`、`__click_test.ps1`）、api-logs-tail.txt、GlassCard 系列组件——经 Glob/LS 确认当前仓库均不存在，无需处理。
2. **保留：记忆系统组件** `api/internal/service/memory/cold_storage_manager.py`、`representation_repulsion.py`——虽生产零引用（仅测试引用），但它们是 PRD（docs/prd/memory-system/）明确规划的 Track B2/C3 特性实现，`memory_settings.py` 中仍有对应配置类，属**在途特性**而非历史遗留废弃代码，删除会破坏项目规划。
3. **保留：hermes/redact.py**——仅测试引用，但属本会话新提交的 Hermes 外部 Agent 适配层组件，可能由桌面端/后续接线使用，暂不删除，待适配层稳定后评估。
4. **保留：仅测试覆盖的组件** `AppTypeSelector.vue`、`DeepThinkingProposalCard.vue`——生产零引用但存在对应 spec 测试（components/__tests__/），按约束"不触碰测试覆盖的组件"保留；如需清理须同步删除 spec。
5. **保留：在用兼容垫片**——app_service.py/workflow_service.py 的 `LEGACY_EXPORT_FORMATS`、api_key_service.py 明文密钥哈希升级、home_service.py 旧缓存回填、account_service.py legacy 会话回退、notification_service.py 旧 key 合并等均有生产执行路径或专项测试，属数据迁移兼容，未删除。
6. **graphify 未更新**：AGENTS.md 要求修改代码后运行 `python -m graphify update .`，但当前 Python 环境未安装 graphify 模块（`No module named graphify`），无法执行；建议在具备该模块的环境补跑。

## 7. 下一步建议（需人工决策）

1. **后端 2 个预先存在失败需修复**（源自批次1提交的功能改动，与本次清理无关）：
   - `test_task_plan_should_dump_summary_without_internal_details`：`execution_orchestration_entity.py` 的 dump 输出新增了 `timeout_seconds: 0.0` 字段，测试期望未同步——需确认该字段应否在 summary 中暴露，二选一修正实现或测试。
   - `test_debug_chat_should_stream_agent_events_and_persist_thoughts`：`app_debug_service.py` 事件流缺少 `execution_summary` 键，测试断言依赖它——需确认调试聊天事件结构。
   - 建议在 main 上单独修复并提交（属于功能改动而非清理范围）。
2. **评审清理分支**：`cleanup/20260813`（2a6aeab）仅供人工评审，确认无误后自行合并到 main（本流程不自动合并/推送）。
3. **记忆系统在途组件**（cold_storage_manager、representation_repulsion）建议在 PRD 对应 Track 落地时接线，届时再评估。
